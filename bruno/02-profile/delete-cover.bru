meta {
  name: Delete cover
  type: http
  seq: 8
}

delete {
  url: {{baseUrl}}/api/profile/cover
  body: none
  auth: inherit
}

headers {
  Origin: {{origin}}
  Accept: application/json
  Cookie: better-auth.session_token={{sessionToken}}
}
