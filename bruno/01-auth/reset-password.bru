meta {
  name: Reset password
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/auth/reset-password
  body: json
  auth: inherit
}

headers {
  Origin: {{origin}}
  Accept: application/json
}

body:json {
    {
    "token": "{{resetToken}}",
    "newPassword": "password456"
  }
}
