meta {
  name: Get group chat
  type: http
  seq: 39
}

get {
  url: {{baseUrl}}/api/v1/study-groups/{{groupId}}/chat
  body: none
  auth: inherit
}

headers {
  Origin: {{origin}}
  Accept: application/json
  Cookie: better-auth.session_token={{sessionToken}}
}
