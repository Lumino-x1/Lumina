meta {
  name: List files
  type: http
  seq: 29
}

get {
  url: {{baseUrl}}/api/v1/study-groups/{{groupId}}/files
  body: none
  auth: inherit
}

headers {
  Origin: {{origin}}
  Accept: application/json
  Cookie: better-auth.session_token={{sessionToken}}
}
