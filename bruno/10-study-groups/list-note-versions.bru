meta {
  name: List note versions
  type: http
  seq: 25
}

get {
  url: {{baseUrl}}/api/v1/study-groups/{{groupId}}/notes/{{noteId}}/versions
  body: none
  auth: inherit
}

headers {
  Origin: {{origin}}
  Accept: application/json
  Cookie: better-auth.session_token={{sessionToken}}
}
