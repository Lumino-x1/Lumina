meta {
  name: List audit events
  type: http
  seq: 40
}

get {
  url: {{baseUrl}}/api/v1/study-groups/{{groupId}}/audit
  body: none
  auth: inherit
}

headers {
  Origin: {{origin}}
  Accept: application/json
  Cookie: better-auth.session_token={{sessionToken}}
}
