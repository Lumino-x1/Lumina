meta {
  name: List timetable versions
  type: http
  seq: 37
}

get {
  url: {{baseUrl}}/api/v1/study-groups/{{groupId}}/timetable/versions
  body: none
  auth: inherit
}

headers {
  Origin: {{origin}}
  Accept: application/json
  Cookie: better-auth.session_token={{sessionToken}}
}
