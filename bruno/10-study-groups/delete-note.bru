meta {
  name: Delete note
  type: http
  seq: 24
}

delete {
  url: {{baseUrl}}/api/v1/study-groups/{{groupId}}/notes/{{noteId}}
  body: none
  auth: inherit
}

headers {
  Origin: {{origin}}
  Accept: application/json
  Cookie: better-auth.session_token={{sessionToken}}
}
