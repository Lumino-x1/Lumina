meta {
  name: Leave group
  type: http
  seq: 10
}

post {
  url: {{baseUrl}}/api/v1/study-groups/{{groupId}}/leave
  body: none
  auth: inherit
}

headers {
  Origin: {{origin}}
  Accept: application/json
  Cookie: better-auth.session_token={{sessionToken}}
}
