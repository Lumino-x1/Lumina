meta {
  name: Accept friend request
  type: http
  seq: 3
}

patch {
  url: {{baseUrl}}/api/friends/request/{{requestId}}/accept
  body: none
  auth: inherit
}

headers {
  Origin: {{origin}}
  Accept: application/json
  Cookie: better-auth.session_token={{sessionToken}}
}
