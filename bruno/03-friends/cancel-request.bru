meta {
  name: Cancel friend request
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/friends/request/{{requestId}}
  body: none
  auth: inherit
}

headers {
  Origin: {{origin}}
  Accept: application/json
  Cookie: better-auth.session_token={{sessionToken}}
}
