meta {
  name: OK ping
  type: http
  seq: 1
  tags: [
    smoke
    workflow
    release-gate
  ]
}

get {
  url: {{baseUrl}}/ok
  body: none
  auth: inherit
}

headers {
  Origin: {{origin}}
  Accept: application/json
}

assert {
  res.status: eq 200
  res.body.message: eq OK
}
