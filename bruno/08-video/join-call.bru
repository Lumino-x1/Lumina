meta {
  name: Join call
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/video/calls/{{callId}}/join
  body: none
  auth: inherit
}

headers {
  Origin: {{origin}}
  Accept: application/json
  Cookie: better-auth.session_token={{sessionToken}}
}
